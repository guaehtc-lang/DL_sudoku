train: img_proc/images/train  # Carpeta de imágenes de entrenamiento
val: img_proc/images/val      # Carpeta de imágenes de validación
test: img_proc/images/test    # Ruta a las imágenes de prueba (opcional)

nc: 1  # Número de clases (ajusta según las clases en 'clases.txt')
names: ['sudoku'] 